import React from "react";
import { createRoot } from "react-dom/client";
import ScholarSpace from "../scholarspace-2.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ScholarSpace />
  </React.StrictMode>
);
